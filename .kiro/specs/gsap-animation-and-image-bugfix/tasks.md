# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Fault Condition** - Hero Elements Blank Screen and Image 404s
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bugs exist
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate both bugs exist
  - **Scoped PBT Approach**: Scope the property to concrete failing cases:
    - Hero elements with `rect.top <= window.innerHeight * 0.3` that remain at `opacity: 0`
    - Images with filter strings in URLs (e.g., `filtersformat(webp)`) that result in 404 errors
  - Test implementation details from Fault Condition in design:
    - Create test HTML page with hero heading at `rect.top = 100px` (viewport height = 800px)
    - Create test HTML page with image URL containing `filtersformat(webp)`
    - Capture pages using UNFIXED code via `captureScript` in `useAppSaveAllResource.js`
    - Assert hero elements should be visible immediately (opacity: 1) - will FAIL on unfixed code
    - Assert images should display correctly - will FAIL on unfixed code with 404 errors
  - The test assertions should match the Expected Behavior Properties from design:
    - Hero elements excluded from scroll-triggered animations (Property 1)
    - Images self-heal on error with CDN fallback (Property 2)
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bugs exist)
  - Document counterexamples found:
    - Hero elements remain at `opacity: 0` for 2+ seconds after page load
    - Images with filter strings show 404 errors in browser console
    - Measure exact timing of when elements become visible
    - Capture browser console errors for image load failures
  - Mark task complete when test is written, run, and failures are documented
  - _Requirements: 2.1, 2.2, 2.3_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Below-Fold Animations and Normal Images
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs:
    - Elements with `rect.top > window.innerHeight * 0.3` animate correctly with GSAP scroll triggers
    - Images without filter strings load correctly from local paths
    - Pre-reveal phase forces visibility, removes scroll hijacks, hides fixed overlays
    - DOM snapshot captures absolute URLs, removes scripts, injects Phantom Engine
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements:
    - **Property 3**: For all elements with `rect.top > window.innerHeight * 0.3`, verify GSAP scroll-triggered animations work identically (opacity and y-axis transitions)
    - **Property 4**: For all images without filter strings, verify they load correctly from local paths
    - **Property 4**: For all pre-reveal and DOM capture operations, verify behavior is unchanged (scroll unlock, visibility forcing, script removal, Phantom Engine injection)
  - Property-based testing generates many test cases for stronger guarantees:
    - Generate random viewport heights (600px to 2000px) and element positions
    - Generate random image URLs (with and without filter strings)
    - Generate random page structures with mixed hero and below-fold content
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 3. Fix for GSAP Animation and Image Bugs

  - [x] 3.1 Add viewport position check in Phantom Engine
    - Modify `captureScript` in `useAppSaveAllResource.js`
    - Add viewport height calculation: `const viewportThreshold = window.innerHeight * 0.3;`
    - Replace `document.querySelectorAll('.cstudio-animate-me')` with filtered selection
    - Use `Array.from()` and `filter()` to exclude elements with `getBoundingClientRect().top <= viewportThreshold`
    - Update animation loop to only animate filtered elements (below-fold content)
    - Preserve existing `gsap.fromTo()` animation configuration and ScrollTrigger settings
    - _Bug_Condition: `(element.classList.contains('cstudio-animate-me') AND element.getBoundingClientRect().top <= window.innerHeight * 0.3)`_
    - _Expected_Behavior: Hero elements excluded from scroll-triggered animations, remain visible with opacity: 1_
    - _Preservation: Below-fold animations continue with identical GSAP behavior (Property 3)_
    - _Requirements: 2.1, 2.3, 3.1_

  - [x] 3.2 Store original image URLs before rewriting
    - Modify the absolute URL forcing phase in `captureScript`
    - Before rewriting image URLs to local paths, store original CDN URL
    - Add: `el.setAttribute('data-original-src', originalUrl)` for all img elements
    - Ensure this happens during the DOM snapshot capture phase
    - _Bug_Condition: `(imageUrl.includes('filters') OR imageUrl.includes('format(') OR imageLoadFails(imageUrl))`_
    - _Expected_Behavior: Original CDN URLs preserved for error handler fallback_
    - _Preservation: Successfully loading images remain unaffected (Property 4)_
    - _Requirements: 2.2, 3.2_

  - [x] 3.3 Inject image error handler for self-healing
    - Add self-healing script in `captureScript` after pre-reveal phase, before Phantom Engine injection
    - Add event listener for `error` event on all `img` elements
    - On error, swap `src` attribute to original CDN URL from `data-original-src`
    - Implementation: `img.addEventListener('error', () => { if (img.dataset.originalSrc) img.src = img.dataset.originalSrc; })`
    - Ensure handler only activates for images that fail to load from local paths
    - _Bug_Condition: `(imageUrl.includes('filters') OR imageUrl.includes('format(') OR imageLoadFails(imageUrl))`_
    - _Expected_Behavior: Images self-heal on 404 by swapping to live CDN URLs (Property 2)_
    - _Preservation: Normal image loading unaffected (Property 4)_
    - _Requirements: 2.2, 3.3_

  - [x] 3.4 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Hero Elements Visible and Images Display
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - Verify hero elements are visible immediately (no blank screen)
    - Verify images with filter strings display correctly via CDN fallback
    - **EXPECTED OUTCOME**: Test PASSES (confirms bugs are fixed)
    - _Requirements: 2.1, 2.2, 2.3 (Expected Behavior Properties from design)_

  - [x] 3.5 Verify preservation tests still pass
    - **Property 2: Preservation** - Below-Fold Animations and Normal Images
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - Verify below-fold animations work identically (Property 3)
    - Verify normal images load correctly (Property 4)
    - Verify pre-reveal and DOM capture operations unchanged (Property 4)
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)

- [x] 4. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
